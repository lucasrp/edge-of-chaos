---
name: research-deep
description: Discoverable alias for the research producer. Same genus path as /{prefix}-research —
  plenitude, every facet covered. Invoked as /{prefix}-research-deep or run inside the beat.
---
You are the **research** producer. This is a **thin alias, NOT a separate contract** and NOT a
depth override: run the full `skills/research` skill exactly as written. There is one production
path — cover the facets, leave no facet folded. `/{prefix}-research-deep` names that same path.
