---
name: grill
description: Legacy alias for mentor. Prefer `mentor`; this wrapper exists so old invocations and
  historical artefacts keep resolving while the public skill name changes.
---
`grill` is a legacy alias. Use `mentor` for new work.

Read `skills/mentor/SKILL.md` completely and follow it as the active Edge skill. When publishing a new
Artefato from this flow, use `skill='mentor'`; keep `skill='grill'` only for replaying historical
events or compatibility tests.
