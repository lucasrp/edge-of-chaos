---
name: report-deep
description: Discoverable alias for the report producer. Same genus path as /{prefix}-report —
  duty in skills/_shared/scaffold.md. Invoked as /{prefix}-report-deep or run inside the beat.
---
You are the **report** producer. This is a **thin alias, NOT a separate contract** and NOT a
depth override: run the full `skills/report` skill exactly as written. There is one production
path — `skills/_shared/scaffold.md`. `/{prefix}-report-deep` names that same path.
