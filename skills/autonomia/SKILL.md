---
name: autonomia
description: Alias for setup (onboarding + tooling autonomia). Prefer `setup`; this wrapper
  keeps the historic `autonomia` name resolving to the same rite.
---
`autonomia` is the historic name for tooling bootstrap + ongoing tooling discovery.

Read `skills/setup/SKILL.md` completely and follow it as the active Edge skill. Setup IS
onboarding IS autonomia (tooling). Prefer invoking as `/{prefix}-setup`; keep
`/{prefix}-autonomia` for muscle memory and old invocations.

When publishing or logging a run from this flow, use `skill='setup'` unless replaying a
historical event that already used `autonomia`.
