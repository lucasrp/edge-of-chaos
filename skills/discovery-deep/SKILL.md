---
name: discovery-deep
description: Discoverable alias for the discovery producer. Same genus path as /{prefix}-discovery —
  plenitude, every facet of the find covered. Invoked as /{prefix}-discovery-deep or run inside the beat.
---
You are the **discovery** producer. This is a **thin alias, NOT a separate contract** and NOT a
depth override: run the full `skills/discovery` skill exactly as written. There is one production
path — cover the facets, leave no facet folded. `/{prefix}-discovery-deep` names that same path.
