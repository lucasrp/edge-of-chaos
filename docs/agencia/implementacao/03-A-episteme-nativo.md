# A — Episteme NATIVO no cortex do edge (migração + teste no roberto)

Ticket A do wayfinder (de-riscado por B+D). O schema completo: `../ontologia-cortex-v2.md` (§1-§3; migração ~180 linhas/5 arquivos + `cortex/schema/ontologia.yaml`). Pré-requisito já no lugar quando chegar aqui: `provenance_class` + CX-1 (feat/provenance-class).

## O mandato (operador, 2026-07-05)
**O episteme integra no edge NATIVAMENTE** — hipótese/pergunta/observation/bearing como nós de 1ª classe do cortex (não um sistema paralelo). O cortex JÁ é a espinha do episteme (eventlog append-only; cortex_provenance asserted/extracted) — re-instanciação BAIXA (H-001).

## Onde se TESTA (operador): no episteme POPULADO do roberto
O roberto pegou o cortex pra construir o episteme (frameworkV9) — o domínio dele É o episteme, e está **bem populado** (dado real: hipóteses/runs/bearings do trabalho jurídico dele). O loop canônico ([[deploy-navigate-4questions-loop]]):
1. build local (TDD, rito codex);
2. deploy no roberto (overlay genótipo, backup, fenótipo intacto — padrão communities);
3. **navegar o episteme REAL dele** via `~/edge/rq` (o probe já instalado) — hipóteses dele viram nós navegáveis? bearings agregam SÓ computed (CX-1 gritando)? a correspondência thread=hipótese=artefato navega em 2-hops?;
4. 3 perguntas: faz sentido / melhorou / cumpriu.

## Guard-rails herdados
- NÃO fabrica :Observation/:Experiment (só quando experimento real rodar — condição do H-001).
- thread=hipótese=artefato = CORRESPONDÊNCIA (3 nós, 2-hop), não identidade.
- gate-verdicts = flat props no :Artefato (B) + aresta `assesses` llm_judged/lead — NUNCA bearing.
- Forward-only, sem backfill. As 3 camadas do domínio do roberto (pesquisa/material/meta) respeitadas — cf. [[communities-loop-roberto-2026-07-05]], #75.
