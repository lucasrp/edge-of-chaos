# Enrichment-Adoption — Evolution / Provenance Trail

Goal: **same information as the book version, visually rich, without trading content for visuals.**
Benchmark = `roteiro-paper-v8` @ commit `eeb696e` (tag `pre-deploy-d3e150f`).

Each artifact snapshot is stamped with its iteration. Pre-trail iterations (before this folder existed)
are reconstructed textually from the codex verdicts; from snapshot files onward, every iteration is a
real file. Folder mirrored to `gdrive:edge/ed/enrichment-evolution/`.

## Goal 1 — Requirements (`enrichment-adoption-requirements.md`)
- iter1: initial draft (force-visuals framing)
- iter2 ← codex pass1 (4 findings): retarget close→descriptor; grounding; env-dependence; residual loophole
- iter3 ← pass2 (3): any_of→min_blocks_of; mandate `visuals.add_visuals` seam; telemetry denominator
- iter4 ← pass3 (3): seam→publish-verifiable; ascii fallback grounded; telemetry publish-event
- iter5 ← pass4 (2): raw-html/svg bypass; remove small-graph exception
- iter6 ← pass5: **APPROVE/SHIP**
- REFRAME (operator evidence: rich lost storytelling vs book):
  - r1 ← reframe pass1 (3): R0 gameable; R1 not subordinated; R0a causal-folklore
  - r2 ← reframe pass2 (3): R0 metrics undefined; R8 self-attest; R2/Q3 contradiction
  - r3: + north-star (content-completeness III); R7 reframed (gains→0 backstop, not low cap)
  - → re-gating to SHIP (full iteration, no cap)

## Goal 2 — Plan (`enrichment-adoption-plan.md`)
- iter1: initial (visual-first ordering — stale)
- iter2: re-derived storytelling-first
- iter3 ← codex pass1 (4): severity-before-cap; forgeable trace; floor-before-grounding; store-before-S3
- iter4 ← codex pass2 (1): R0-class must block from day1
- iter5: P1 RESOLVED (evidence live at producer→close seam = wiring); de-cap; → re-gating to SHIP

## Goal 3 — Slices (in progress)
Sequence: `S1 → S3a → (S3‖S4) → S2 → S5 → S7 → S6 → (S8‖S9‖S10)`.
Each slice: dogfood artifact (what the build needs) → TDD → /codex:review full-iteration → snapshot.
- (pending)
