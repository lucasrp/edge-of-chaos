---
name: ed-assemble
description: Edge wrapper for assemble. Select @ed-assemble in the skills picker (or ask for `ed-assemble` by name) to follow the canonical skills/assemble/SKILL.md contract.
---
Select this skill as `@ed-assemble` (or name `ed-assemble` in the prompt). Then read `skills/assemble/SKILL.md` completely and follow it as the active Edge skill. This wrapper exists only to expose the repo-local Codex skill name `ed-assemble`; do not duplicate or reinterpret the canonical contract here.
