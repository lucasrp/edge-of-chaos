---
name: edge-calibrate
description: Edge wrapper for calibrate. Select @edge-calibrate in the skills picker (or ask for `edge-calibrate` by name) to follow the canonical skills/calibrate/SKILL.md contract.
---
Select this skill as `@edge-calibrate` (or name `edge-calibrate` in the prompt). Then read `skills/calibrate/SKILL.md` completely and follow it as the active Edge skill. This wrapper exists only to expose the repo-local Codex skill name `edge-calibrate`; do not duplicate or reinterpret the canonical contract here.
