---
name: edge-consolidate
description: Edge wrapper for consolidate. Select @edge-consolidate in the skills picker (or ask for `edge-consolidate` by name) to follow the canonical skills/consolidate/SKILL.md contract.
---
Select this skill as `@edge-consolidate` (or name `edge-consolidate` in the prompt). Then read `skills/consolidate/SKILL.md` completely and follow it as the active Edge skill. This wrapper exists only to expose the repo-local Codex skill name `edge-consolidate`; do not duplicate or reinterpret the canonical contract here.
