---
name: edge-prototype
description: Edge wrapper for prototype. Select @edge-prototype in the skills picker (or ask for `edge-prototype` by name) to follow the canonical skills/prototype/SKILL.md contract.
---
Select this skill as `@edge-prototype` (or name `edge-prototype` in the prompt). Then read `skills/prototype/SKILL.md` completely and follow it as the active Edge skill. This wrapper exists only to expose the repo-local Codex skill name `edge-prototype`; do not duplicate or reinterpret the canonical contract here.
