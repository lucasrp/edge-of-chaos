---
name: edge-beat
description: Edge wrapper for beat. Select @edge-beat in the skills picker (or ask for `edge-beat` by name) to follow the canonical skills/beat/SKILL.md contract.
---
Select this skill as `@edge-beat` (or name `edge-beat` in the prompt). Then read `skills/beat/SKILL.md` completely and follow it as the active Edge skill. This wrapper exists only to expose the repo-local Codex skill name `edge-beat`; do not duplicate or reinterpret the canonical contract here.
