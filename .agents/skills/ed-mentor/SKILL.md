---
name: ed-mentor
description: Edge wrapper for mentor. Select @ed-mentor in the skills picker (or ask for `ed-mentor` by name) to follow the canonical skills/mentor/SKILL.md contract.
---
Select this skill as `@ed-mentor` (or name `ed-mentor` in the prompt). Then read `skills/mentor/SKILL.md` completely and follow it as the active Edge skill. This wrapper exists only to expose the repo-local Codex skill name `ed-mentor`; do not duplicate or reinterpret the canonical contract here.
