---
name: edge-quente
description: Edge wrapper for quente. Select @edge-quente in the skills picker (or ask for `edge-quente` by name) to follow the canonical skills/quente/SKILL.md contract.
---
Select this skill as `@edge-quente` (or name `edge-quente` in the prompt). Then read `skills/quente/SKILL.md` completely and follow it as the active Edge skill. This wrapper exists only to expose the repo-local Codex skill name `edge-quente`; do not duplicate or reinterpret the canonical contract here.
