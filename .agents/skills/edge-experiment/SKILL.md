---
name: edge-experiment
description: Edge wrapper for experiment. Select @edge-experiment in the skills picker (or ask for `edge-experiment` by name) to follow the canonical skills/experiment/SKILL.md contract.
---
Select this skill as `@edge-experiment` (or name `edge-experiment` in the prompt). Then read `skills/experiment/SKILL.md` completely and follow it as the active Edge skill. This wrapper exists only to expose the repo-local Codex skill name `edge-experiment`; do not duplicate or reinterpret the canonical contract here.
