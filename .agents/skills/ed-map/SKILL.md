---
name: ed-map
description: Edge wrapper for map. Select @ed-map in the skills picker (or ask for `ed-map` by name) to follow the canonical skills/map/SKILL.md contract.
---
Select this skill as `@ed-map` (or name `ed-map` in the prompt). Then read `skills/map/SKILL.md` completely and follow it as the active Edge skill. This wrapper exists only to expose the repo-local Codex skill name `ed-map`; do not duplicate or reinterpret the canonical contract here.
