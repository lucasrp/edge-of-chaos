---
name: ed-recall
description: Edge wrapper for recall. Select @ed-recall in the skills picker (or ask for `ed-recall` by name) to follow the canonical skills/recall/SKILL.md contract.
---
Select this skill as `@ed-recall` (or name `ed-recall` in the prompt). Then read `skills/recall/SKILL.md` completely and follow it as the active Edge skill. This wrapper exists only to expose the repo-local Codex skill name `ed-recall`; do not duplicate or reinterpret the canonical contract here.
