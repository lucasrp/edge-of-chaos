---
name: ed-consolidate
description: Edge wrapper for consolidate. Select @ed-consolidate in the skills picker (or ask for `ed-consolidate` by name) to follow the canonical skills/consolidate/SKILL.md contract.
---
Select this skill as `@ed-consolidate` (or name `ed-consolidate` in the prompt). Then read `skills/consolidate/SKILL.md` completely and follow it as the active Edge skill. This wrapper exists only to expose the repo-local Codex skill name `ed-consolidate`; do not duplicate or reinterpret the canonical contract here.
