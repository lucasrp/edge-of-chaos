---
name: ed-prototype
description: Edge wrapper for prototype. Select @ed-prototype in the skills picker (or ask for `ed-prototype` by name) to follow the canonical skills/prototype/SKILL.md contract.
---
Select this skill as `@ed-prototype` (or name `ed-prototype` in the prompt). Then read `skills/prototype/SKILL.md` completely and follow it as the active Edge skill. This wrapper exists only to expose the repo-local Codex skill name `ed-prototype`; do not duplicate or reinterpret the canonical contract here.
