---
name: edge-grill
description: Edge wrapper for grill. Select @edge-grill in the skills picker (or ask for `edge-grill` by name) to follow the canonical skills/grill/SKILL.md contract.
---
Select this skill as `@edge-grill` (or name `edge-grill` in the prompt). Then read `skills/grill/SKILL.md` completely and follow it as the active Edge skill. This wrapper exists only to expose the repo-local Codex skill name `edge-grill`; do not duplicate or reinterpret the canonical contract here.
