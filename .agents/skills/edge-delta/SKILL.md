---
name: edge-delta
description: Edge wrapper for delta. Select @edge-delta in the skills picker (or ask for `edge-delta` by name) to follow the canonical skills/delta/SKILL.md contract.
---
Select this skill as `@edge-delta` (or name `edge-delta` in the prompt). Then read `skills/delta/SKILL.md` completely and follow it as the active Edge skill. This wrapper exists only to expose the repo-local Codex skill name `edge-delta`; do not duplicate or reinterpret the canonical contract here.
