---
name: edge-critique
description: Edge wrapper for critique. Select @edge-critique in the skills picker (or ask for `edge-critique` by name) to follow the canonical skills/critique/SKILL.md contract.
---
Select this skill as `@edge-critique` (or name `edge-critique` in the prompt). Then read `skills/critique/SKILL.md` completely and follow it as the active Edge skill. This wrapper exists only to expose the repo-local Codex skill name `edge-critique`; do not duplicate or reinterpret the canonical contract here.
