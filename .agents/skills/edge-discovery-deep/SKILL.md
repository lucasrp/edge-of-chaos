---
name: edge-discovery-deep
description: Edge wrapper for discovery-deep. Select @edge-discovery-deep in the skills picker (or ask for `edge-discovery-deep` by name) to follow the canonical skills/discovery-deep/SKILL.md contract.
---
Select this skill as `@edge-discovery-deep` (or name `edge-discovery-deep` in the prompt). Then read `skills/discovery-deep/SKILL.md` completely and follow it as the active Edge skill. This wrapper exists only to expose the repo-local Codex skill name `edge-discovery-deep`; do not duplicate or reinterpret the canonical contract here.
