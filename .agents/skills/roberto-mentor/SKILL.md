---
name: roberto-mentor
description: Edge wrapper for mentor. Select @roberto-mentor in the skills picker (or ask for `roberto-mentor` by name) to follow the canonical skills/mentor/SKILL.md contract.
---
Select this skill as `@roberto-mentor` (or name `roberto-mentor` in the prompt). Then read `skills/mentor/SKILL.md` completely and follow it as the active Edge skill. This wrapper exists only to expose the repo-local Codex skill name `roberto-mentor`; do not duplicate or reinterpret the canonical contract here.
