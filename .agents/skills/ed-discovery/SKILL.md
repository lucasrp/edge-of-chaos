---
name: ed-discovery
description: Edge wrapper for discovery. Select @ed-discovery in the skills picker (or ask for `ed-discovery` by name) to follow the canonical skills/discovery/SKILL.md contract.
---
Select this skill as `@ed-discovery` (or name `ed-discovery` in the prompt). Then read `skills/discovery/SKILL.md` completely and follow it as the active Edge skill. This wrapper exists only to expose the repo-local Codex skill name `ed-discovery`; do not duplicate or reinterpret the canonical contract here.
