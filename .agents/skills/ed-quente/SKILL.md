---
name: ed-quente
description: Edge wrapper for quente. Select @ed-quente in the skills picker (or ask for `ed-quente` by name) to follow the canonical skills/quente/SKILL.md contract.
---
Select this skill as `@ed-quente` (or name `ed-quente` in the prompt). Then read `skills/quente/SKILL.md` completely and follow it as the active Edge skill. This wrapper exists only to expose the repo-local Codex skill name `ed-quente`; do not duplicate or reinterpret the canonical contract here.
