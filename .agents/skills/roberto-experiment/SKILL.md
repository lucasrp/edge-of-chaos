---
name: roberto-experiment
description: Edge wrapper for experiment. Select @roberto-experiment in the skills picker (or ask for `roberto-experiment` by name) to follow the canonical skills/experiment/SKILL.md contract.
---
Select this skill as `@roberto-experiment` (or name `roberto-experiment` in the prompt). Then read `skills/experiment/SKILL.md` completely and follow it as the active Edge skill. This wrapper exists only to expose the repo-local Codex skill name `roberto-experiment`; do not duplicate or reinterpret the canonical contract here.
