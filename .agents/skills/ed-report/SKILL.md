---
name: ed-report
description: Edge wrapper for report. Select @ed-report in the skills picker (or ask for `ed-report` by name) to follow the canonical skills/report/SKILL.md contract.
---
Select this skill as `@ed-report` (or name `ed-report` in the prompt). Then read `skills/report/SKILL.md` completely and follow it as the active Edge skill. This wrapper exists only to expose the repo-local Codex skill name `ed-report`; do not duplicate or reinterpret the canonical contract here.
