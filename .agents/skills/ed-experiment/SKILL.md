---
name: ed-experiment
description: Edge wrapper for experiment. Select @ed-experiment in the skills picker (or ask for `ed-experiment` by name) to follow the canonical skills/experiment/SKILL.md contract.
---
Select this skill as `@ed-experiment` (or name `ed-experiment` in the prompt). Then read `skills/experiment/SKILL.md` completely and follow it as the active Edge skill. This wrapper exists only to expose the repo-local Codex skill name `ed-experiment`; do not duplicate or reinterpret the canonical contract here.
