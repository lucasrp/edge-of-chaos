---
name: edge-mentor
description: Edge wrapper for mentor. Select @edge-mentor in the skills picker (or ask for `edge-mentor` by name) to follow the canonical skills/mentor/SKILL.md contract.
---
Select this skill as `@edge-mentor` (or name `edge-mentor` in the prompt). Then read `skills/mentor/SKILL.md` completely and follow it as the active Edge skill. This wrapper exists only to expose the repo-local Codex skill name `edge-mentor`; do not duplicate or reinterpret the canonical contract here.
