---
name: ed-wake
description: Edge wrapper for wake. Select @ed-wake in the skills picker (or ask for `ed-wake` by name) to follow the canonical skills/wake/SKILL.md contract.
---
Select this skill as `@ed-wake` (or name `ed-wake` in the prompt). Then read `skills/wake/SKILL.md` completely and follow it as the active Edge skill. This wrapper exists only to expose the repo-local Codex skill name `ed-wake`; do not duplicate or reinterpret the canonical contract here.
