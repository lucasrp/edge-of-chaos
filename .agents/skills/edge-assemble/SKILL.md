---
name: edge-assemble
description: Edge wrapper for assemble. Select @edge-assemble in the skills picker (or ask for `edge-assemble` by name) to follow the canonical skills/assemble/SKILL.md contract.
---
Select this skill as `@edge-assemble` (or name `edge-assemble` in the prompt). Then read `skills/assemble/SKILL.md` completely and follow it as the active Edge skill. This wrapper exists only to expose the repo-local Codex skill name `edge-assemble`; do not duplicate or reinterpret the canonical contract here.
