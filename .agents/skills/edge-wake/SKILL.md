---
name: edge-wake
description: Edge wrapper for wake. Select @edge-wake in the skills picker (or ask for `edge-wake` by name) to follow the canonical skills/wake/SKILL.md contract.
---
Select this skill as `@edge-wake` (or name `edge-wake` in the prompt). Then read `skills/wake/SKILL.md` completely and follow it as the active Edge skill. This wrapper exists only to expose the repo-local Codex skill name `edge-wake`; do not duplicate or reinterpret the canonical contract here.
