---
name: edge-report
description: Edge wrapper for report. Select @edge-report in the skills picker (or ask for `edge-report` by name) to follow the canonical skills/report/SKILL.md contract.
---
Select this skill as `@edge-report` (or name `edge-report` in the prompt). Then read `skills/report/SKILL.md` completely and follow it as the active Edge skill. This wrapper exists only to expose the repo-local Codex skill name `edge-report`; do not duplicate or reinterpret the canonical contract here.
