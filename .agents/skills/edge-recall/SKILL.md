---
name: edge-recall
description: Edge wrapper for recall. Select @edge-recall in the skills picker (or ask for `edge-recall` by name) to follow the canonical skills/recall/SKILL.md contract.
---
Select this skill as `@edge-recall` (or name `edge-recall` in the prompt). Then read `skills/recall/SKILL.md` completely and follow it as the active Edge skill. This wrapper exists only to expose the repo-local Codex skill name `edge-recall`; do not duplicate or reinterpret the canonical contract here.
