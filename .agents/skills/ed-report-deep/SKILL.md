---
name: ed-report-deep
description: Edge wrapper for report-deep. Select @ed-report-deep in the skills picker (or ask for `ed-report-deep` by name) to follow the canonical skills/report-deep/SKILL.md contract.
---
Select this skill as `@ed-report-deep` (or name `ed-report-deep` in the prompt). Then read `skills/report-deep/SKILL.md` completely and follow it as the active Edge skill. This wrapper exists only to expose the repo-local Codex skill name `ed-report-deep`; do not duplicate or reinterpret the canonical contract here.
