---
name: ed-calibrate
description: Edge wrapper for calibrate. Select @ed-calibrate in the skills picker (or ask for `ed-calibrate` by name) to follow the canonical skills/calibrate/SKILL.md contract.
---
Select this skill as `@ed-calibrate` (or name `ed-calibrate` in the prompt). Then read `skills/calibrate/SKILL.md` completely and follow it as the active Edge skill. This wrapper exists only to expose the repo-local Codex skill name `ed-calibrate`; do not duplicate or reinterpret the canonical contract here.
