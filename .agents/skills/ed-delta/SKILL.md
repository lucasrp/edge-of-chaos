---
name: ed-delta
description: Edge wrapper for delta. Select @ed-delta in the skills picker (or ask for `ed-delta` by name) to follow the canonical skills/delta/SKILL.md contract.
---
Select this skill as `@ed-delta` (or name `ed-delta` in the prompt). Then read `skills/delta/SKILL.md` completely and follow it as the active Edge skill. This wrapper exists only to expose the repo-local Codex skill name `ed-delta`; do not duplicate or reinterpret the canonical contract here.
